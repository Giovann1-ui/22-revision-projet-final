# login utilisateur (auto login, pas d'inscription) (PRIORITE)
creation de table utilisateur (pseudo, )
2 ou 3 utilisateurs a creer en base
login avec champ pseudo (si pseudo existe, on log l'utilisateur, sinon on creer l'utilisateur)

# envoi message entre utilisateurs

# système de message lu et non lu

# mettre un bouton "rafraichir" pour avoir les nouveux messages.